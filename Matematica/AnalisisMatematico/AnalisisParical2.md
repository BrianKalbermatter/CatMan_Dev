# Continuidad:

# Continuidad de las Funciones Derivables:
Para una funcion, la propiedad de ser derivable es mas fuerte que la de ser continua. Es decir, la derivabilidad asegura la continuidad, mientras que el reciproco no se cumple, pues existen funciones continuas en un punto que no son derivables en el.
Si es Derivable entonces es continua pero no al reves. Si es Continua no puede ser Derivable.

Teorema: 
	Si una funcion tiene derivada finita en un punto, entonces es continua en dicho punto.

## Funcion Derivada:
- El valor f'(a) considerado como imagen para cada punto a del Dominio, donde f(x) es derivable, permite definir una nueva funcion f'(x), que se llama funcion derivada de f(x).

- La derivada de una funcion mide:
Como cambia la funcion cuando cambia la variable independiente.
- Es la pendiente de la recta tangente a la curva en un punto.
- Te dice la velocidad de cambio instantanea.

🤙 <La derivada de f(x) en un punto 'a' se define como el limite:>
f'(a)= Lim(h -> 0) f(a + h) - f(a)
		  ----------------
			h 
📌 Esta fraccion se llama cociente incremental.
- El numerador: diferencia de valores de la funcion.
- El denominador: diferencia en las x.
Cuando h es muy pequeno, esa fraccion se acerca a la pendiente de la recta tangente en el punto.

## Derivabilidad y Continuidad:



# Derivadas Laterales:








# Parametros:

# Funciones Racional Asintotas:

# Asintotas: 
Vertical, horizontal y oblicuas.

1 grado mayor el numerador (tiene que diferir 1 grado no dos ni 3).
Verticales, horizontales y oblicuas
- Asintotas Vertical: f(x) va a tener asintota vertical <=> L(x -> a) f(x) = +- infinito para calcular a tengo que hacer 0 al denominador
- para que exista oblicua tengo que ir descartando las otras asintotas.

Asintota Horizontal: **f(x) <=> L (x->infinito) f(x) = L (finito).**
Indeterminado divido un numero muy grande por uno muy chico da 0

# Cotas, Extremos y Elementos:

# Valor Absoluto:

# Funcion Inversa:




